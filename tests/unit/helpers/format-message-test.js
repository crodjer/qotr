import { formatMessage } from '../../../helpers/format-message';
import { module, test } from 'qunit';

module('Unit | Helper | format message');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatMessage(42);
  assert.ok(result);
});
