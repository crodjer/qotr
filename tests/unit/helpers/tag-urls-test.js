import { tagUrls } from '../../../helpers/tag-urls';
import { module, test } from 'qunit';

module('Unit | Helper | tag urls');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tagUrls(42);
  assert.ok(result);
});
