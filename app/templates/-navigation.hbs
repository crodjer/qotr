<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'home' class="navbar-brand" }}QOTR{{/link-to }}
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>{{#link-to 'channels.new' }}New{{/link-to }}</li>
      </ul>

      {{#if model }}
      <ul class="nav navbar-nav navbar-right">
        <li>
          <a class="cursor glyphicon glyphicon-edit" {{ action "editNick" }} >
            <strong>{{ model.nick }}</strong>
          </a>
        </li>
        {{!--
        <li>
          {{# if model.connected }}
            <a class="cursor glyphicon glyphicon-log-out"
              {{ action "disconnect" }}>
              Disconnect
            </a>
          {{ else }}
            <a class="cursor glyphicon glyphicon-log-in"
              {{ action "connect" }}>
              Connect
            </a>
          {{/if }}
        </li>
        --}}
      </ul>
      {{/if }}
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
